{
  "scripts": {
    "build": "tsc && yarn parcel build build/sidepanel.js build/background.js",
    "clear": "rm -rf build/ dist/",
    "package": "zip extension.zip manifest.json sidepanel.html dist/ css/ images/"
  },
  "dependencies": {
    "@material/web": "^1.0.0-pre.15",
    "lit": "^2.7.6"
  },
  "devDependencies": {
    "chrome-types": "^0.1.219",
    "parcel": "^2.9.3",
    "typescript": "^5.1.6"
  }
}
